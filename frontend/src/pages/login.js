import { h } from '../lib/format.js';
export function loginPage(config) {
  return `<main class="login-layout"><section class="login-story"><a class="brand" href="#"><span class="brand-mark">R</span>RailPlan<span class="brand-tag">STARTER</span></a><div class="story-main"><div class="eyebrow">MAINTENANCE COORDINATION</div><h1>One window.<br>Many moving parts.</h1><p>A shared workspace for maintenance requests, clear conflict checks and reviewable schedules.</p><div class="story-flow"><span>Request</span><i>&rarr;</i><span>Check</span><i>&rarr;</i><span>Plan</span><i>&rarr;</i><span>Approve</span></div></div><div class="story-footer">Synthetic railway &middot; No live operational connections</div></section><section class="login-form-wrap"><div class="login-card"><div class="eyebrow dark">TEAM WORKSPACE</div><h2>${config.auth_mode==='demo'?'Choose a demo profile':'Sign in to RailPlan'}</h2><p class="muted">One website. Two roles. Separate responsibilities.</p><div id="notice" role="alert"></div>${config.auth_mode==='demo'?`<div class="demo-profiles">${config.demo_users.map((u)=>`<button class="profile-card" data-demo="${h(u.id)}"><span class="profile-icon">${u.role==='officer'?'O':'R'}</span><span><strong>${h(u.name)}</strong><small>${u.role==='officer'?'Review all work, resolve conflicts and publish':'Submit work and track your own requests'}</small></span><span class="profile-arrow">&rarr;</span></button>`).join('')}</div><div class="notice neutral"><strong>Local demo only.</strong> These buttons select fictional profiles. They are not secure authentication. Enable Supabase before a shared deployment.</div>`:`<form id="login-form"><label>Email<input name="email" type="email" autocomplete="username" required /></label><label>Password<input name="password" type="password" autocomplete="current-password" required /></label><button class="button primary full" type="submit">Sign in</button></form><p class="small muted">Accounts are invited by your team administrator. Your officer/requester role is assigned by the backend, not on this screen.</p>`}<p class="login-help">Getting started? See <code>README.md</code> and <code>docs/TEAM_WORK.md</code>.</p></div></section></main>`;
}
